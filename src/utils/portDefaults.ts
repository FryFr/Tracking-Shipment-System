export const PORT_ETA_DEFAULTS: Record<string, number> = {
    'Ensenada': 3,
    'Long Beach': 4,
    'Laredo': 2,
    'Manzanillo': 3,
    'Houston': 3,
};

export const PORT_OPTIONS = Object.keys(PORT_ETA_DEFAULTS);
